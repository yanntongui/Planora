
// Firebase configuration disabled.
// Reverting to local state management for Auth.

export const auth = null;
export const db = null;
export const storage = null;

const app = null;
export default app;
